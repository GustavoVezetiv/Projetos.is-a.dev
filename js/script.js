/* ARQUIVO: script.js */

// --- ESTADO DO JOGO --- //
let rollCount = parseInt(localStorage.getItem('rollCount')) || 0; 
let isRolling = false;
/* script.js legado removido. O site agora é página única (index.html) e não usa gating por dado.
   Mantido apenas como placeholder intencionalmente vazio. */